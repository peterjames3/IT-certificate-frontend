"use client";

import React, { useState } from "react";
import { motion, AnimatePresence } from "framer-motion";
import {
  Shield,
  Search,
  ArrowRight,
  BookOpen,
  Users,
  Clock,
  Award,
  ChevronRight,
  CheckCircle2,
  Globe,
  Lock,
  BarChart3,
  FileCheck,
  Server,
} from "lucide-react";

// ─── Types ───────────────────────────────────────────────
interface Certification {
  id: string;
  code: string;
  title: string;
  description: string;
  icon: React.ReactNode;
  domains: string[];
  level: "core" | "advanced";
}

// ─── Data ────────────────────────────────────────────────
const certifications: Certification[] = [
  {
    id: "cisa",
    code: "CISA",
    title: "Certified Information Systems Auditor",
    description:
      "IT audit, governance, risk management, control frameworks, compliance monitoring, and information systems assurance.",
    icon: <FileCheck className="w-6 h-6" />,
    domains: ["Auditing", "Governance", "Risk", "Compliance"],
    level: "core",
  },
  {
    id: "cism",
    code: "CISM",
    title: "Certified Information Security Manager",
    description:
      "Information security governance, risk management, incident response, security program development, and compliance oversight.",
    icon: <Shield className="w-6 h-6" />,
    domains: ["Security", "Governance", "Incident Response", "Compliance"],
    level: "core",
  },
  {
    id: "crisc",
    code: "CRISC",
    title: "Certified in Risk and Information Systems Control",
    description:
      "Enterprise risk management, IT risk identification, risk response, control monitoring, and information systems control frameworks.",
    icon: <BarChart3 className="w-6 h-6" />,
    domains: ["Risk ID", "Risk Response", "Control Monitoring", "Frameworks"],
    level: "core",
  },
  {
    id: "cgeit",
    code: "CGEIT",
    title: "Certified in the Governance of Enterprise IT",
    description:
      "Enterprise IT governance frameworks, strategic alignment, value delivery, resource optimization, and performance measurement.",
    icon: <Globe className="w-6 h-6" />,
    domains: ["Governance", "Strategy", "Value Delivery", "Resources"],
    level: "advanced",
  },
  {
    id: "cdpse",
    code: "CDPSE",
    title: "Certified Data Privacy Solutions Engineer",
    description:
      "Privacy governance, data lifecycle management, privacy architecture, and emerging privacy-enhancing technologies.",
    icon: <Lock className="w-6 h-6" />,
    domains: ["Privacy", "Data Lifecycle", "Architecture", "Technology"],
    level: "advanced",
  },
  {
    id: "csx",
    code: "CSX-P",
    title: "Cybersecurity Practitioner",
    description:
      "Hands-on cybersecurity skills, threat detection, incident response, and network security implementation.",
    icon: <Server className="w-6 h-6" />,
    domains: ["Threat Detection", "Incident Response", "Network Security", "Implementation"],
    level: "advanced",
  },
];

const stats = [
  { label: "Expert Instructors", value: "20+", icon: <Users className="w-4 h-4" /> },
  { label: "Certification Tracks", value: "6", icon: <BookOpen className="w-4 h-4" /> },
  { label: "Hours Support", value: "24", icon: <Clock className="w-4 h-4" /> },
  { label: "Exam Success Rate", value: "99%", icon: <Award className="w-4 h-4" /> },
];

const categoryList = [
  { name: "Core Certifications", count: 3, active: true },
  { name: "Advanced Certifications", count: 3, active: false },
  { name: "Exam Proxy Services", count: 6, active: false },
  { name: "Domain Mastery", count: 24, active: false },
];

// ─── Components ────────────────────────────────────────────
function StatBadge({ stat }: { stat: (typeof stats)[0] }) {
  return (
    <div className="flex items-center gap-2 text-blue-100/80 text-sm font-medium">
      <span className="p-1.5 rounded-lg bg-white/10 text-cyan-300">{stat.icon}</span>
      <span className="text-white font-semibold">{stat.value}</span>
      <span className="hidden sm:inline">{stat.label}</span>
    </div>
  );
}

function CategoryItem({
  cat,
  isActive,
  onClick,
}: {
  cat: (typeof categoryList)[0];
  isActive: boolean;
  onClick: () => void;
}) {
  return (
    <motion.button
      whileHover={{ x: 4 }}
      onClick={onClick}
      className={`w-full group flex items-center justify-between p-4 rounded-2xl transition-all duration-300 border ${
        isActive
          ? "bg-white/15 border-white/20 shadow-lg shadow-blue-900/20"
          : "bg-white/5 border-white/5 hover:bg-white/10 hover:border-white/10"
      }`}
    >
      <div className="flex items-center gap-3">
        <span
          className={`w-2 h-2 rounded-full transition-colors ${
            isActive ? "bg-cyan-400" : "bg-white/20 group-hover:bg-white/40"
          }`}
        />
        <span
          className={`text-sm font-semibold transition-colors ${
            isActive ? "text-white" : "text-blue-100/70 group-hover:text-white"
          }`}
        >
          {cat.name}
        </span>
      </div>
      <div className="flex items-center gap-2">
        <span className="text-xs font-medium text-blue-200/50 bg-white/5 px-2 py-0.5 rounded-full">
          {cat.count}
        </span>
        <ChevronRight
          className={`w-4 h-4 transition-all ${
            isActive
              ? "text-cyan-300 translate-x-0 opacity-100"
              : "text-white/20 -translate-x-1 opacity-0 group-hover:translate-x-0 group-hover:opacity-100"
          }`}
        />
      </div>
    </motion.button>
  );
}

function CertificationCard({ cert }: { cert: Certification }) {
  return (
    <motion.div
      layout
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, scale: 0.95 }}
      whileHover={{ y: -4 }}
      className="group relative flex flex-col h-full bg-white rounded-2xl border border-slate-100 shadow-sm hover:shadow-xl hover:shadow-blue-900/10 transition-all duration-500 overflow-hidden"
    >
      {/* Top accent bar */}
      <div className="h-1.5 w-full bg-gradient-to-r from-blue-600 via-cyan-500 to-blue-400" />

      <div className="p-6 flex flex-col flex-1">
        {/* Header */}
        <div className="flex items-start justify-between mb-4">
          <div className="flex items-center gap-3">
            <div className="p-2.5 rounded-xl bg-gradient-to-br from-blue-50 to-cyan-50 text-blue-600 border border-blue-100">
              {cert.icon}
            </div>
            <div>
              <span className="inline-block px-2 py-0.5 rounded-md bg-amber-50 text-amber-700 text-[10px] font-bold tracking-wider uppercase border border-amber-100 mb-1">
                {cert.code}
              </span>
              <h3 className="text-lg font-bold text-slate-800 leading-tight">
                {cert.title}
              </h3>
            </div>
          </div>
        </div>

        {/* Description */}
        <p className="text-slate-500 text-sm leading-relaxed mb-5 flex-1">
          {cert.description}
        </p>

        {/* Domains */}
        <div className="flex flex-wrap gap-2 mb-5">
          {cert.domains.map((domain) => (
            <span
              key={domain}
              className="px-2.5 py-1 rounded-lg bg-slate-50 text-slate-600 text-xs font-medium border border-slate-100"
            >
              {domain}
            </span>
          ))}
        </div>

        {/* CTA */}
        <button className="mt-auto w-full flex items-center justify-center gap-2 py-3 px-4 rounded-xl bg-gradient-to-r from-blue-600 to-blue-500 text-white text-sm font-semibold shadow-lg shadow-blue-500/25 hover:shadow-blue-500/40 hover:from-blue-500 hover:to-cyan-500 transition-all duration-300 group/btn">
          <span>Hire Exam Taker For This Certification</span>
          <ArrowRight className="w-4 h-4 transition-transform group-hover/btn:translate-x-1" />
        </button>
      </div>
    </motion.div>
  );
}

// ─── Main Page ─────────────────────────────────────────────
export default function ISACACertificationMatrix() {
  const [activeTab, setActiveTab] = useState<"core" | "advanced">("core");
  const [activeCategory, setActiveCategory] = useState(0);

  const filteredCerts = certifications.filter((c) => c.level === activeTab);

  return (
    <main className="min-h-screen bg-slate-50">
      {/* ═══════════════════════════════════════════
          HERO SECTION — Blue Gradient (Design 2 Style)
          ═══════════════════════════════════════════ */}
      <section className="relative overflow-hidden bg-gradient-to-br from-[#051024] via-[#0a1f3d] to-[#123a5c]">
        {/* Decorative background elements */}
        <div className="absolute inset-0">
          <div className="absolute top-0 right-0 w-[600px] h-[600px] bg-blue-500/10 rounded-full blur-3xl -translate-y-1/2 translate-x-1/4" />
          <div className="absolute bottom-0 left-0 w-[500px] h-[500px] bg-cyan-400/5 rounded-full blur-3xl translate-y-1/3 -translate-x-1/4" />
          <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] bg-blue-400/5 rounded-full blur-3xl" />
          {/* Grid pattern overlay */}
          <div
            className="absolute inset-0 opacity-[0.03]"
            style={{
              backgroundImage: `linear-gradient(rgba(255,255,255,0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.1) 1px, transparent 1px)`,
              backgroundSize: "60px 60px",
            }}
          />
        </div>

        <div className="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-8 pb-20 lg:pb-28">
          {/* Top Stats Bar */}
          <motion.div
            initial={{ opacity: 0, y: -10 }}
            animate={{ opacity: 1, y: 0 }}
            className="flex flex-wrap items-center justify-center gap-4 sm:gap-8 mb-12 py-3 px-6 rounded-full bg-white/5 border border-white/10 backdrop-blur-sm w-fit mx-auto"
          >
            {stats.map((stat) => (
              <StatBadge key={stat.label} stat={stat} />
            ))}
          </motion.div>

          {/* Main Hero Content */}
          <div className="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
            {/* Left Column — Text & Categories */}
            <motion.div
              initial={{ opacity: 0, x: -30 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.6 }}
            >
              <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-cyan-400/10 border border-cyan-400/20 text-cyan-300 text-xs font-bold tracking-widest uppercase mb-6">
                <span className="w-1.5 h-1.5 rounded-full bg-cyan-400 animate-pulse" />
                Most Ordered ISACA Certification Tracks
              </div>

              <h1 className="text-4xl sm:text-5xl lg:text-6xl font-extrabold text-white leading-[1.1] mb-6 tracking-tight">
                ISACA IT Governance{" "}
                <span className="text-transparent bg-clip-text bg-gradient-to-r from-cyan-300 to-blue-300">
                  & Security
                </span>{" "}
                Certification Matrix
              </h1>

              <p className="text-blue-100/60 text-lg leading-relaxed mb-10 max-w-lg">
                Select your target ISACA certification to review how our
                professional proctored exam takers map out specific exam domains
                and handle complex scenario-based questions.
              </p>

              {/* Category List (Design 2 Style) */}
              <div className="space-y-3">
                <h2 className="text-white/40 text-xs font-bold uppercase tracking-widest mb-4">
                  Browse Our Categories
                </h2>
                {categoryList.map((cat, idx) => (
                  <CategoryItem
                    key={cat.name}
                    cat={cat}
                    isActive={activeCategory === idx}
                    onClick={() => {
                      setActiveCategory(idx);
                      if (idx === 0) setActiveTab("core");
                      if (idx === 1) setActiveTab("advanced");
                    }}
                  />
                ))}
              </div>
            </motion.div>

            {/* Right Column — Image Card */}
            <motion.div
              initial={{ opacity: 0, x: 30 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.6, delay: 0.2 }}
              className="relative hidden lg:block"
            >
              <div className="relative">
                {/* Main image container */}
                <div className="relative rounded-[2.5rem] bg-gradient-to-br from-blue-400/20 to-cyan-300/10 border border-white/10 p-2 backdrop-blur-sm">
                  <div className="rounded-[2rem] overflow-hidden bg-gradient-to-br from-[#87ceeb]/30 to-[#b0e0e6]/20 aspect-[4/5] relative">
                    {/* Placeholder for the person image — replace src with your actual image */}
                    <div className="absolute inset-0 flex items-center justify-center">
                      <div className="text-center p-8">
                        <div className="w-48 h-48 mx-auto mb-6 rounded-full bg-gradient-to-br from-blue-300/20 to-cyan-200/20 border-2 border-white/20 flex items-center justify-center">
                          <Shield className="w-20 h-20 text-white/30" />
                        </div>
                        <p className="text-white/40 text-sm font-medium">
                          Your certification success
                          <br />
                          starts here
                        </p>
                      </div>
                    </div>
                    {/* Floating badges */}
                    <motion.div
                      animate={{ y: [0, -8, 0] }}
                      transition={{ duration: 4, repeat: Infinity, ease: "easeInOut" }}
                      className="absolute top-8 left-8 px-4 py-2 rounded-xl bg-white/10 backdrop-blur-md border border-white/20 text-white text-xs font-bold shadow-lg"
                    >
                      <div className="flex items-center gap-2">
                        <CheckCircle2 className="w-4 h-4 text-cyan-300" />
                        CISA Verified
                      </div>
                    </motion.div>
                    <motion.div
                      animate={{ y: [0, 8, 0] }}
                      transition={{ duration: 5, repeat: Infinity, ease: "easeInOut", delay: 1 }}
                      className="absolute bottom-12 right-8 px-4 py-2 rounded-xl bg-white/10 backdrop-blur-md border border-white/20 text-white text-xs font-bold shadow-lg"
                    >
                      <div className="flex items-center gap-2">
                        <Award className="w-4 h-4 text-amber-300" />
                        99% Pass Rate
                      </div>
                    </motion.div>
                  </div>
                </div>

                {/* Vertical text decoration (like design 2) */}
                <div className="absolute -right-8 top-1/2 -translate-y-1/2 flex flex-col gap-6">
                  {["CISA", "CISM", "CRISC", "CGEIT"].map((text, i) => (
                    <span
                      key={text}
                      className="text-[10px] font-bold tracking-widest text-white/10 uppercase writing-mode-vertical"
                      style={{ writingMode: "vertical-rl" }}
                    >
                      {text}
                    </span>
                  ))}
                </div>
              </div>
            </motion.div>
          </div>
        </div>

        {/* Bottom fade to white */}
        <div className="absolute bottom-0 left-0 right-0 h-24 bg-gradient-to-t from-slate-50 to-transparent" />
      </section>

      {/* ═══════════════════════════════════════════
          CERTIFICATION CARDS SECTION
          ═══════════════════════════════════════════ */}
      <section className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 -mt-10 relative z-10 pb-24">
        {/* Tabs */}
        <div className="flex justify-center mb-12">
          <div className="inline-flex p-1.5 rounded-full bg-white border border-slate-200 shadow-lg shadow-slate-200/50">
            <button
              onClick={() => setActiveTab("core")}
              className={`relative px-8 py-3 rounded-full text-sm font-bold transition-all duration-300 ${
                activeTab === "core"
                  ? "text-white shadow-md"
                  : "text-slate-500 hover:text-slate-700"
              }`}
            >
              {activeTab === "core" && (
                <motion.div
                  layoutId="activeTab"
                  className="absolute inset-0 bg-gradient-to-r from-blue-600 to-blue-500 rounded-full"
                  transition={{ type: "spring", bounce: 0.2, duration: 0.6 }}
                />
              )}
              <span className="relative z-10">Core Certifications</span>
            </button>
            <button
              onClick={() => setActiveTab("advanced")}
              className={`relative px-8 py-3 rounded-full text-sm font-bold transition-all duration-300 ${
                activeTab === "advanced"
                  ? "text-white shadow-md"
                  : "text-slate-500 hover:text-slate-700"
              }`}
            >
              {activeTab === "advanced" && (
                <motion.div
                  layoutId="activeTab"
                  className="absolute inset-0 bg-gradient-to-r from-blue-600 to-blue-500 rounded-full"
                  transition={{ type: "spring", bounce: 0.2, duration: 0.6 }}
                />
              )}
              <span className="relative z-10">Advanced Certifications</span>
            </button>
          </div>
        </div>

        {/* Cards Grid */}
        <AnimatePresence mode="wait">
          <motion.div
            key={activeTab}
            initial={{ opacity: 0, y: 10 }}
            animate={{ opacity: 1, y: 0 }}
            exit={{ opacity: 0, y: -10 }}
            transition={{ duration: 0.3 }}
            className="grid md:grid-cols-2 lg:grid-cols-3 gap-6"
          >
            {filteredCerts.map((cert) => (
              <CertificationCard key={cert.id} cert={cert} />
            ))}
          </motion.div>
        </AnimatePresence>

        {/* Bottom CTA */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="mt-16 text-center"
        >
          <div className="inline-flex flex-col sm:flex-row items-center gap-4 p-6 rounded-3xl bg-gradient-to-r from-blue-600 to-blue-500 text-white shadow-xl shadow-blue-500/20">
            <Search className="w-6 h-6 text-cyan-200" />
            <p className="text-sm sm:text-base font-medium">
              Need help choosing the right certification track?
            </p>
            <button className="px-5 py-2.5 rounded-xl bg-white/15 hover:bg-white/25 border border-white/20 text-sm font-bold transition-colors backdrop-blur-sm">
              Talk to an Expert
            </button>
          </div>
        </motion.div>
      </section>
    </main>
  );
}